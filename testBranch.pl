#!/usr/bin/perl

=head2

 create branch

 git branch testing 	# create a branch named testing
			# now still on master branch
 git checkout testing	# switch to testing breach
			# now you can write code on testing branch

 git branch		# list all branches
			# current branch will have a *

 * modified the file attribute using chmod command will affect the 
   changed, should commit the file again

   the changed attribute will not affect the file attribute in other
   branch

=cut
